\section{Introduction and Context}
\label{sec:introduction}

This section first introduces the intrusion-detection problem and the scope of the study. It then explains why graph and language representations are combined, states the research questions and contributions, and finally outlines the report.

Internet of Things (IoT) networks connect sensors, cameras, appliances and industrial controllers. Their traffic must be monitored for attacks while legitimate services continue to operate. Missed attacks can leave harmful activity unchallenged, while false alarms consume analyst attention. Alert validation and threat prioritisation are therefore practical requirements of intrusion monitoring~\citep{scarfone2007idps}.

Conventional machine learning provides a way to classify traffic from measurements such as duration, byte count, protocol and destination port. When each connection is represented as an independent row, its surrounding communication pattern is not represented directly. Deep learning can learn richer feature representations, but a model operating on individual rows still needs an explicit way to use relationships between connections. This motivates moving from isolated traffic records to a graph representation.

Graph neural networks (GNNs) represent communicating devices as nodes and their connections as edges. They can combine a connection's attributes with information from neighbouring devices and links, as in graph-based intrusion detectors such as E-GraphSAGE~\citep{lo2022egraphsage}. This context is relevant when an attack is expressed across many connections: a scan may contact many hosts, while a distributed attack may converge on one destination. Graph structure supplies a view that an isolated flow record does not directly contain.

Pretrained language models offer a complementary representation of an event described in text. Large language models (LLMs) can generate explanations, while encoder models such as BERT produce representations that can support classification~\citep{devlin2019bert}. This study uses CySecBERT, a cybersecurity-adapted BERT encoder, to represent short templated descriptions of traffic~\citep{bayer2024cysecbert}. It remains frozen and does not generate text. Its embeddings are another representation of the recorded flow attributes, not an independent source of observations.

Previous work has combined graph and language models in several ways. Parallel encoders with attention-based fusion have been studied in misinformation detection~\citep{moorthy2025dualstream}. In intrusion detection, XG-NID uses a GNN for classification and a generative LLM for explanations and remedial guidance~\citep{farrukh2025xgnid}. DAS already uses iterative graph--language feedback for node-level learning~\citep{thapaliya2025das}. Thus, neither fusion nor iterative feedback is new by itself; their value in the present edge-level detection task must be established through comparison and diagnostic tests.

This report develops GLASS (Graph-Language Adaptive Semantic-Structural fusion). GLASS represents aggregated flows as directed edges, learns their structural context through a graph attention network, encodes their text descriptions with frozen CySecBERT, and combines the embeddings using a gate that varies by edge and feature dimension. Its feedback stage consults a semantic classifier on uncertain edges and injects the advice before another graph update. We evaluate the structural, prototype semantic, fusion and feedback stages, compare prototype and trained-head consultants, and measure the trained head alone. Tests that disable output fusion examine whether injection itself helps. The same pipeline is evaluated on NF-UNSW-NB15 and NF-ToN-IoT under the graph-construction assumptions in Section~\ref{sec:method-graph}; the scores are controlled comparisons, not deployment estimates.

NF-UNSW-NB15 derives from UNSW-NB15, whose original benchmark was created from a mixture
of normal activity and contemporary attacks in the UNSW Canberra Cyber Range
Laboratory~\citep{moustafa2015unsw}. NF-ToN-IoT derives from the TON\_IoT datasets and
provides an IoT-focused telemetry setting~\citep{moustafa2021toniot}. This study uses both
in their NetFlow-standardised forms~\citep{sarhan2021netflow}. One identical processing
and modelling pipeline is applied to the two datasets. Every architectural choice is
shared between them. Only two quantities selected on validation data differ.

The two datasets were chosen because they occupy very different points on the
class-imbalance axis. After aggregation, NF-UNSW-NB15 contains 656 edges, is 47.4\%
benign, and has a most-to-least-frequent class ratio of $12.4{:}1$. Eight of its nine
attack classes contain 40 edges and the ninth contains 25, so only one falls at or below
35 edges. NF-ToN-IoT contains 2{,}127 edges, is 82.1\% benign, and has a most-to-least-frequent
class ratio of $582{:}1$. Seven of its nine attack classes contain 35 edges or fewer. Its
distribution is far more skewed. By the most-to-least class ratio, the two
graphs differ by roughly a factor of 47 in imbalance severity. That ratio counts the two
NF-ToN-IoT classes the metric excludes, \texttt{dos} and \texttt{ransomware}; over the classes
the metric scores it is $145.5{:}1$, still roughly twelve times NF-UNSW-NB15's. The model faces
the whole graph, so this report quotes the graph-level ratio, and the two descriptors that do not
depend on that choice point the same way: 82.1\% of NF-ToN-IoT's edges are benign against
47.4\%, and seven of its nine attack classes hold 35 edges or fewer against one.

That difference is the methodological reason for using two datasets. A single dataset
cannot separate a property of the architecture from a property of the class distribution
that architecture happened to face. Holding the architecture fixed while changing the
imbalance regime by roughly $47\times$ makes that distinction experimentally addressable.
The two graphs are not matched on everything else, and we do not claim they are. They also
differ in the share of edges that repeat an ordered address pair, 58.7\% against 7.9\%, and
in size and density, at 49 nodes and 656 edges against 1{,}501 and 2{,}127. Two datasets
cannot separate those axes from imbalance, and Section~\ref{sec:res-attention} uses them in
their own right.
RQ4 already asks how class imbalance affects fusion and feedback, and the two-dataset design
gives that question two substantially different regimes to compare. No stage is assumed in
advance to be sensitive or insensitive to that change.

The scoring protocol also differs in one necessary respect. Macro-F1 covers ten classes on
NF-UNSW-NB15 and eight classes on NF-ToN-IoT, because two NF-ToN-IoT classes are too small
to survive five-fold splitting. Their edges remain in the graph for message passing but
are excluded from the metric. Because the class counts differ, absolute scores are not
comparable across the two datasets. Only the shape of the model ladder is comparable, that
ladder being the four stages of this study placed in order, each compared against the one below
it. This constraint applies throughout the report.

\subsection{Why combine graph and language representations?}

Graph neural networks (GNNs) provide a direct way to represent communication structure.
In this work, IP addresses form the nodes of a directed graph and aggregated flows form
labelled edges. A graph-attention encoder propagates information between communicating
hosts and produces a structural representation for each edge. This branch can learn
patterns that are difficult to express as independent rows in a conventional feature
table. It does not, however, possess an explicit vocabulary for interpreting the traffic.
The graph tells the model \emph{who communicated with whom} and how that interaction is
positioned in the network. It does not explain what a high-frequency TCP
connection to a particular service may signify.

A pretrained cybersecurity language encoder offers a complementary view. The same
aggregated flow can be written as a short, controlled sentence describing its endpoints,
protocol, port, frequency, volume, and duration. The implementation uses CySecBERT, a
BERT-class model adapted to cybersecurity text and evaluated by its authors on
domain-specific representation and classification tasks~\citep{bayer2024cysecbert}. It is
frozen and used purely as a sentence encoder. Throughout this report, this component is
called the \emph{semantic branch}. That terminology distinguishes its representational
role from that of a generative large language model. Where the same component is evaluated on
its own as one stage of the comparison, it is called the semantic model.

Neither representation is assumed to win in every case; the design instead lets them
challenge and complement one another. The parallel fusion stage runs
the graph and semantic encoders side by side and combines their edge embeddings through
Adaptive Gated Attention Fusion, referred to throughout as the fusion model. The feedback stage adds uncertainty-guided
semantic feedback: edges with high-entropy GNN predictions are selected for semantic
consultation, and the resulting signal is reintroduced by modifying the edge
representations that the next graph update consumes. A second design, which biases graph
attention directly, was also implemented and measured. It produced no change in prediction
at all, and is reported as a verified negative result.

The coupling is bidirectional in routing. The graph model's per-edge uncertainty decides
which edges are consulted, while the semantic branch returns a per-edge judgement that
re-enters the edge representations consumed by the next graph update. It is not
bidirectional in content: the consultant's judgement depends on that edge's text alone,
not on the graph model's current belief about the edge.

The distinction from the prior work introduced above is the edge-level intrusion-detection setting and its controlled evaluation. GLASS tests sample-adaptive fusion and uncertainty-guided consultation, then separates the performance of the complete configuration from that of its semantic consultant and injection path. Section~\ref{sec:related} develops the comparison with earlier systems.

\subsection{Research problem and questions}

The research problem is not simply whether adding a language embedding raises a score.
If one branch is already a strong classifier, a fusion module can appear successful while
doing little more than copying that branch. Likewise, a feedback loop can improve the
final output because semantic features are fused at the classifier, even when the claimed
attention re-entry mechanism contributes nothing. The evaluation must separate
the value of the two modalities, the fusion mechanism, the uncertainty-selection policy,
and the iterative update itself.

Five research questions frame the study:

\begin{description}
    \item[RQ1.] How do the structural GNN branch and the semantic branch perform
    individually on NF-UNSW-NB15 and NF-ToN-IoT under the same five-fold out-of-fold
    evaluation protocol?

    \item[RQ2.] Does the fusion model score above both single-branch rungs on NF-UNSW-NB15 and
    NF-ToN-IoT, and are those differences separated under the bootstrap protocol, meaning that
    their intervals exclude zero?

    \item[RQ3.] Does uncertainty-guided semantic feedback add value beyond static fusion
    on NF-UNSW-NB15 and NF-ToN-IoT? If so, does the gain come from semantic re-entry into
    the graph representation or from final-output fusion?

    \item[RQ4.] How do graph topology, repeated endpoint pairs, class imbalance, and the
    strength of the semantic consultant affect fusion and feedback on NF-UNSW-NB15 and
    NF-ToN-IoT?

    \item[RQ5.] How does the feedback model compare against published graph-based
    intrusion detection architectures re-trained on the same aggregated representation
    under the same protocol on NF-UNSW-NB15 and NF-ToN-IoT, and how much of any observed
    difference is attributable to representation and tuning rather than to architecture?
\end{description}

These questions deliberately allow a negative answer. The experiments are meant to determine
when the proposed interaction works, when it does not, and why. A model ladder of GNN, semantic
encoder, fusion and then feedback is only meaningful if each higher rung is compared fairly
with the strongest relevant lower rung.

\subsection{Objectives and contributions}

The practical objective of GLASS is to build one reproducible pipeline that transforms
NF-UNSW-NB15 and NF-ToN-IoT NetFlow records into two aligned views and evaluates every
stage under one consistent protocol. The work makes five contributions.

\begin{enumerate}
    \item It implements a directed, edge-classification graph pipeline in which flows are
    aggregated by source IP, destination IP, and attack class, with centrality-based node
    features and statistical edge attributes.

    \item It serialises each edge as a controlled-language description using a fixed sentence template. The text supplied to the encoder is kept
    label-free by an explicit validation guard, preventing the semantic branch from seeing
    the target attack name or a synonymous relation label.

    \item It implements and compares two multimodal stages: the fusion model for parallel structural--
    semantic fusion, and uncertainty-guided semantic feedback that reintroduces semantic
    evidence into the edge representations of uncertain edges. The feedback model consults
    a classification head trained on the semantic embeddings. The feedback model is reported under that
    consultant and under the weaker embedding-only scorer it was first built with, so the
    reader can see how much of the result is the mechanism and how much is the consultant.

    \item It provides a controlled evaluation on NF-UNSW-NB15 and NF-ToN-IoT, including
    unimodal baselines, fusion comparisons, uncertainty-selection ablations, a comparison
    between the two consultants, mechanism-isolation tests, and two published graph-based
    intrusion detection architectures re-trained on the same representation. It reports both
    positive and negative results to show which components carry the measured gain,
    including five measured attempts to make an embedding-only consultant's advice usable
    by the feedback channel, none of which succeeded.

    \item It provides a cross-dataset comparison under one shared architecture across two
    substantially different class-imbalance regimes, testing whether the behaviour of each
    stage in the model ladder is a property of the method or of the class distribution it
    faces.
\end{enumerate}

The central result is that a strong semantic classifier explains much of the complete system's performance, while an added benefit from feedback injection remains unestablished. The trained-head feedback configuration is separated above the GNN, semantic and fusion stages and the faithful re-trained baselines on both datasets. However, the trained head alone has a slightly higher mean macro-F1 on both, with no separated difference from feedback. The prototype-feedback configuration does not establish a gain over either structural stage. Changing the consultant also changed the consultation percentile, so the increase between configurations cannot be assigned to the consultant alone.

The mechanism tests make this distinction explicit. With output fusion disabled, neither realistic consultant gives a separated improvement through injection at the earlier consultation settings tested; the trained head produces a separated loss on NF-ToN-IoT. An oracle with access to the true label does improve through the same path. Thus the channel can carry useful information, but the tested consultants do not establish an injection benefit. The contribution is the implementation and controlled evaluation of this design, including these negative results and the limits of their interpretation.

\subsection{Report structure}

The following sections review related detection and fusion methods (Section~\ref{sec:related}), describe GLASS and its evaluation protocol (Section~\ref{sec:method}), present model comparisons and diagnostic results (Section~\ref{sec:results}), interpret findings and discuss their limitations (Section~\ref{sec:discussion}), and conclude with findings and future directions (Section~\ref{sec:conclusion}); the appendices provide supporting measurements and reproducibility details.
